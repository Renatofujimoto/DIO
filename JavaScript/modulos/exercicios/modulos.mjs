export function showAge(nome, idade) {
  return `A idade de ${nome} e ${idade}.`
}

export function showCity(nome, cidade) {
  return `A cidade de ${nome} e ${cidade}!`
}

export function showHooby(nome, hobby) {
  return `O hobby de ${nome} e ${hobby}!`
}
